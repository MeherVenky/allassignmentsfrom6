class Solution{
    public static long find_multiplication (int arr[], int brr[], int n, int m) {
         Arrays.sort(arr);
        Arrays.sort(brr);
       long product=(arr[n-1]*brr[0]);
        return product;
    }

}
